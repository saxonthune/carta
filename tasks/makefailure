#!/bin/bash

FAILURES_DIR="./tasks/failures"

# Ensure failures directory exists
mkdir -p "$FAILURES_DIR"

# Get datetime in yyyyMMdd_hhmm format
DATETIME=$(date +"%Y%m%d_%H%M")

# Write the failure details
echo "$*" > "$FAILURES_DIR/$DATETIME.txt"
echo "Created $FAILURES_DIR/$DATETIME.txt"
