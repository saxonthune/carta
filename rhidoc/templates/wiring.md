## Documentation

This repo uses a `{{dir_name}}/` spec workspace — numbered Markdown docs with stable
cross-references and a generated index.

- Read `{{dir_name}}/AGENTS.md` for how to navigate and edit the workspace.
- Read `{{dir_name}}/MANIFEST.md` for the doc index.
- Run `rhidoc ai-skill` before using the `rhidoc` CLI.
